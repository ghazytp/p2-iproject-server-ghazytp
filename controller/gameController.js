const { Game } = require('../models')
import axios from 'axios'

class GameController {
    static async showGameData(req, res, next) {
        try {
            
        } catch (err) {
            next(err)    
        }
    }
}